// App-wide constants including route definitions
export const ROUTES = {
  HOME: '/',
  FUN: '/fun',
  RESOURCES: '/resources',
};

// Used to determine major tab switches (no animations)
export const MAJOR_TABS = [
  ROUTES.HOME,
  ROUTES.FUN,
  ROUTES.RESOURCES,
];
